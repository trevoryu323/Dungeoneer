package entity;

// this is the top-level entity class, from which all entities derive from

public abstract class AEntity {
    protected String name; // entity name
    protected int level;  // entity's level
    public AStats stat;  // entity's stats
    
}